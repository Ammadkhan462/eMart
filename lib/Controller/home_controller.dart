import 'package:get/get.dart';

//this is created beacause our home screen is stateless widget so if we use
//getx in home than we got nothing

class Homecontroller extends GetxController {
  var currentvarindex = 0.obs;
}
